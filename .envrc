use flake

# Export GitHub credentials from gh CLI for Gradle (Morphe registry)
export GITHUB_TOKEN="$(gh auth token 2>/dev/null || true)"
export GITHUB_ACTOR="$(gh api user --jq .login 2>/dev/null || true)"

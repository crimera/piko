# X-Lite Improvement TODO

Target APK: `12.17.3-alpha.01`. Do **not** add compatibility aliases, legacy shims, or version-gated fallbacks for obfuscated names — resolve at patch time or don't ship it. Workstream letters reference `X_LITE_PATCH_IMPROVEMENT_PLAN.md` (B, C, D, E, F).

## 1. [x] Simplify SemanticIntrospection duplicated direct/helper walks (Plan B)

- Deduplicate the direct-resolution and helper-walk paths in `patches/src/main/kotlin/app/crimera/patches/xlite/models/SemanticIntrospection.kt` without weakening required cardinality.
- Keep helper/property discovery gated by the same cardinality policy; do not widen partial matches.
- **Evidence:** `:patches:test` and `:patches:assemble` pass; the exact `12.17.3-alpha.01` production run applied all 24 patches. Direct/helper field resolution now shares one register-to-field walk without changing the cardinality gate.

## 2. Inspect Morphe patcher 1.6.0 source before touching ScopedFingerprintMatching (Plan C2)

- Inspect the exact 1.6.0 dependency/source artifact used by this repo; confirm what ScopedFingerprintMatching actually changes before any decision to keep or drop it.
- Benchmark/validate against a fingerprint baseline so the decision is measured, not assumed.
- **Evidence:** notes of the 1.6.0 diff; before/after patch-time numbers; decision recorded with justification.

## 3. Reassess five-resolver granularity and repeated base fingerprint rematches (Plan A/D)

- Re-evaluate the five resolver/cache seams (workstream A) and stop repeat rematches of the same base fingerprint per patch run.
- Preserve narrow failure dependencies: a resolver must fail only where the name it resolves was already required; never bundle unrelated lookups.
- **Evidence:** timing showing each base fingerprint matched at most once per run; failure surface (which patches break together) unchanged vs. staged state.

## 4. Audit/reduce makeFieldsPublic mutation blast radius (Plan F)

- Bound `makeFieldsPublic` to the minimal set of fields a patch actually mutates; verify generated bytecode in the final DEX.
- **Evidence:** mutation call sites listed vs. fields actually widened (verified against the produced DEX, not assumed); runtime smoke of patched app (no VerifyError/IllegalAccessError).

## 5. [x] InlineDownloadButton ThreadLocal cleanup on exception (Plan E4)

- Wrap the inline-action renderer in a catch-all that calls `finishRender()` before rethrowing; keep normal cleanup outside the protected range so cleanup failures cannot recurse into the handler.
- **Evidence:** `:extensions:xlite:test`, `:patches:test`, and `:patches:build` pass; the exact alpha patch applies exclusively; final DEX contains the reachable catch-all cleanup and rethrow; the installed APK launches, renders the normal-size download icon, and survives repeated recomposition without `VerifyError` or runtime-boundary errors. The exception-path unit test exercises cleanup before native icon selection.

## 6. Complete remaining plan phases

- [x] Settings contribution initialization plumbing:
  - Prepare the `SettingsRegistry.load()` target once per `BytecodePatchContext`.
  - Emit compatible category/group registrations once per patch run while keeping setting items feature-local.
  - Preserve contribution dependencies and parent-before-child registration order.
  - Evidence: `:patches:test` and `:patches:assemble` pass; commit `9c9d0002`.
- [x] Settings renderer/resolution implementation:
  - Resolve the Compose row caller and renderer once in `SettingsPatch.kt`.
  - Inject the X-Lite settings row and use the runtime `SettingsRenderer` catalog.
  - Runtime verification completed with `./verify-xlite-settings.sh`: the patched APK launches and the X-Lite settings screen works without crashing.
- [x] Runtime settings lifecycle and read boundaries:
  - **Register once during initialization.** Keep `SettingsRegistry.load()` (and its injected category/group/item/option/configuration calls) as the single registration sequence. Invoke it once from the application/extension initialization path, after the host context and resources are ready and before any settings query. Do not register or configure settings from render callbacks, filters, click handlers, or per-item code. Registration must finish by freezing the registry; a second load/registration attempt is a lifecycle error, not a recovery path.
  - **Snapshot per outer operation.** At the entry of each operation that processes a collection, tree, batch, or render pass, read every setting needed by that operation into local immutable values/a snapshot object. Do this before entering the item loop or recursive helpers, then pass the snapshot down. Never call `SettingsRegistry.getBoolean`, `getString`, or `getStringSet` from a per-item loop, nested item helper, or recursive traversal. A `Set` snapshot must be defensively copied if its source can change.
  - **Keep operation semantics stable.** A setting change during an operation must not make the first and last item use different policy. Re-read at the next outer operation boundary so changes can take effect; do not turn ordinary settings into a process-wide cache. Settings marked `rebootApp` may retain their existing restart-based behavior.
  - **Audit boundaries and likely call sites.** Treat one timeline-filter invocation as one boundary (flags/rule/source values flow through every post, module, and child); one `InlineDownloadButton.handleEvent` as one action boundary and one `enqueueAllDownloads` call as one batch (read conflict behavior once, then pass it to every enqueue); `MediaPickerDialog.show` as one row-build boundary (read copy-link once before the row loop); and one dynamic-color/font render or transformation pass as one boundary (capture theme/font flags before token/character iteration). Scalar resolvers such as `MediaTabResolver` still read at most once per invocation.
  - **Evidence:** `:extensions:xlite:test`, `:patches:test`, and `:patches:assemble` pass. The implementation guards registry registration, snapshots multi-download conflict behavior per batch, snapshots theme settings before media-row construction, and snapshots dynamic-color enablement before accent-tone injection. The exact `12.17.3-alpha.01` APK patches successfully and the user confirmed the patched runtime works. Call-count instrumentation remains optional follow-up evidence.
- Premium/download and simple owner-scoped UI audits — scope each to its own owner; do not assume crossover.
- Separated cold vs. warm patch timings (Plan D2) so shared-dependency costs are attributable.
- Full production/runtime matrix (production APK + runtime, not just test harness).
- **Evidence:** per-phase checklists closed; cold/warm numbers separated per patch; matrix rows green for the audited UI paths.

---

### Standing rules
- Scope every search narrowly; never repo-wide greps (see repo AGENTS.md).